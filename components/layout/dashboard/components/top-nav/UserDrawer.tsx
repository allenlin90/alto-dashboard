import { useAuth } from 'hooks/useAuth';

export const UserDrawer: React.FC = () => {
  const [auth] = useAuth();

  // TODO: define user setting darwer UI and business logic
  return <></>;
};

export default UserDrawer;
