import type { NotificationsProps } from '../Notifications';

export const NotificationDrawer: React.FC<NotificationsProps> = ({
  notifications,
}) => {
  // TODO: define business logic on how to mark notification as read
  return <></>;
};

export default NotificationDrawer;
